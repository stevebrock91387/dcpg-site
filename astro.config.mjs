// @ts-check
import { defineConfig } from 'astro/config';

// Static output (default) → GitHub Pages. `site` drives canonical URLs + sitemap.
export default defineConfig({
  site: 'https://decoupagescreenwriting.com',
});
